[mergetool]
	keepBackup = true
[core]
	editor = atom --wait
	excludesfile = ~/.gitignore
[credential]
	helper = osxkeychain
[push]
	default = simple
[filter "lfs"]
	clean = git lfs clean %f
	smudge = git lfs smudge %f
	required = true
